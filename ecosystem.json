{
    /**
     * Application configuration section
     * http://pm2.keymetrics.io/docs/usage/application-declaration/
     */
    "apps" : [
        {
            "name" : "etobee-admin-staging",
            "script" : "app.js",
            "env": {
                "NODE_ENV": "staging"
            }
        }
    ],

    /**
     * Deployment section
     * http://pm2.keymetrics.io/docs/usage/deployment/
     */
    "deploy" : {
        "staging" : {
            "user" : "ubuntu",
            "host" : "staging.admin2.etobee.com",
            "ref" : "origin/development",
            "repo": "git@gitlab.com:etobee-techgroup/etobee-admin.git",
            "path" : "/home/ubuntu/etobee-admin-staging",
            "post-deploy" : "npm install && bower install && PORT=3001 pm2 startOrRestart ecosystem.json --env staging"
        }
    }
}
